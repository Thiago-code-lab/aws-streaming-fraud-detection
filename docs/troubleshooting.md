# Solução de problemas

- `ModuleNotFoundError`: rode `python -m pip install -r requirements.txt`.
- `pyarrow` ausente: reinstale dependências em Python 3.11+.
- Terraform pede `unique_suffix`: edite `terraform.tfvars`.
- Sem credenciais AWS: use a demonstração local; ela não precisa de AWS.
